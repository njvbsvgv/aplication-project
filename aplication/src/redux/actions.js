import { navigationSlice } from "./navigationSlice";
import { registerDataSlice } from "./registerDataSlice";

export const { changeNaigationLink } = navigationSlice.actions
export const { changeRegisterSteper } = registerDataSlice.actions